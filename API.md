# API

## GET /health
Returns service and network status.

## GET /vaults/:id
Returns treasury composition.

## GET /agents/decisions
Returns latest agent decisions and confidence scores.

## POST /policies
Creates or updates governance policy.
